/**
 * @file func.c
 * 
 * int func( pointer at the position of % as *pp, ...)
 * if *(pp + 1 = specifier)
 *  call spec.c
 * 
 * else if (its = %)
 *  return %
 * 
 * else( which means = state)
 *   call state()
 */